<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('partials/commonHeadTags') %>
    <title>Join VIP</title>
    <link rel="stylesheet" href="/styles/partials/form.css" />
    <link rel="stylesheet" href="/styles/simple-form.css" />
  </head>
  <body>
    <%- include('partials/navbar') %>
    <main>
      <h1>Join VIP</h1>

      <% if (locals.error) { %>
      <p><%= error %></p>
      <% } %>

      <form action="join-vip" method="post" class="form">
        <div class="form-field">
          <label for="secret">Enter the secret key</label>
          <input
            type="password"
            id="secret"
            name="secret"
            placeholder="********"
            required
          />
        </div>
        <button class="btn" type="submit">
          <%- include('icons/send') %> Join
        </button>
      </form>
    </main>
  </body>
</html>
