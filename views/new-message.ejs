<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('partials/commonHeadTags') %>
    <title>New message</title>
    <link rel="stylesheet" href="/styles/partials/form.css" />
    <link rel="stylesheet" href="/styles/simple-form.css" />
  </head>
  <body>
    <%- include('partials/navbar') %>
    <main>
      <h1>Add a new message</h1>

      <% if (locals.error) { %>
      <p><%= locals.error %></p>
      <% } %>

      <form action="new-message" method="post" class="form">
        <div class="form-field">
          <label for="title">Title</label>
          <input
            type="text"
            id="title"
            name="title"
            placeholder="Enter the title"
            required
            maxlength="20"
            value="<%= locals.formData?.title %>"
          />
        </div>
        <div class="form-field">
          <label for="content">Content</label>
          <textarea
            id="content"
            name="content"
            placeholder="Enter the content"
            required
          >
<%= locals.formData?.content %></textarea
          >
        </div>
        <button class="btn" type="submit">
          <%- include('icons/send') %> Send
        </button>
      </form>
    </main>
  </body>
</html>
